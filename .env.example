# PostgreSQL (main branch — зөвхөн локал DB-д хамаарна)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=research123
POSTGRES_DB=shop

# JWT — variant branch дээр backend-т өгнө
JWT_SECRET=research-jwt-secret

# Datadog trial API key (хоосон үед `--profile datadog` идэвхгүй)
DD_API_KEY=
DD_SITE=datadoghq.com

# Frontend — main branch дээр backend-руу заагаагүй. Variant branch дээр өөрчилнө.
# Жишээ: nestjs-raw variant branch дээр http://backend-nestjs-raw:3001 болгоно.
NEXT_PUBLIC_API_BASE=
