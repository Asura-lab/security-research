# security-research — nestjs-orm variant

NestJS + Prisma ORM (parameterized). Port 3002.

## Ажиллуулах

```bash
cp .env.example .env
docker compose up --build
```

- Frontend: http://localhost:3000
- Backend:  http://localhost:3002
- Postgres: :5432

Seed:

```bash
python db/seed.py
```

Attacks/contracts/monitoring — main branch-т.
